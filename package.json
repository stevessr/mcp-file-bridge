{
  "name": "mcp-file-bridge",
  "version": "0.3.0",
  "private": true,
  "description": "ChatGPT/Codex App file bridge using host-native file parameters, with direct HTTP streaming fallback.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
