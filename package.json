{
  "name": "mcp-file-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "A small remote MCP server for uploading sandbox artifacts without echoing file contents into chat.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
