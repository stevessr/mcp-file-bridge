{
  "name": "mcp-file-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "MCP control plane with direct streaming HTTP uploads for large sandbox artifacts.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
